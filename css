@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  color-scheme: dark;
}

html,
body {
  min-height: 100%;
  background: #050505;
  color: #ffffff;
}

body {
  margin: 0;
}

* {
  box-sizing: border-box;
}

::selection {
  background: #e50914;
  color: #ffffff;
}
